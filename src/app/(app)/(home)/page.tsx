import PodsPage from "@/app/(app)/pods/page";

// The homepage is the pods experience: signed-in users land on their pods,
// visitors get the sign-in / discover call-to-action.
export const dynamic = "force-dynamic";

export default PodsPage;
